paths:
  root_dir: "data/kitti"
  train: "train"
  test: "test"

preprocessing:
  rgb_mean: [0.485, 0.456, 0.406]  # ImageNet mean
  rgb_std: [0.229, 0.224, 0.225]   # ImageNet std
  fill_depth: true  # Enable depth interpolation
  
  target_size: [256, 512]  # [H, W] for resizing