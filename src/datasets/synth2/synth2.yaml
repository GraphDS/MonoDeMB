paths:
  root_dir: data/synth2_dataset
  train: train
  val: val
  test: test

preprocessing:
  rgb_mean: [0.485, 0.456, 0.406]  # ImageNet mean
  rgb_std: [0.229, 0.224, 0.225]   # ImageNet std
  target_size: [384, 512]          # [H, W]
  fill_depth: false